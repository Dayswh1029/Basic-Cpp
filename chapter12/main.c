#include<stdio.h>
#include"Matrix.h"




int main()
{
    Matrix *matA= createMatrix(2,3);
    Matrix *matB= createMatrix(2,3);
    Matrix *matC= createMatrix(2,3);
    Matrix *matD= createMatrix(2,3);
    Matrix *matNUll=NULL;

    //初始化

    matA->data[3]=2.3f;
    matB->data[3]=3.1f;





    

    return 0;
}
